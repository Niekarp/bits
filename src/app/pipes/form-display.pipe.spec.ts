import { FormDisplayPipe } from './form-display.pipe';

describe('FormDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
